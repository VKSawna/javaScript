const num1 = 50

const num2 = -124

const num3 = 459.5986
console.log(Math);

console.log(Math.abs(num2)) // Absolute value of num2

console.log(Math.round(num3))

console.log(Math.ceil(num3)) // Round up to the nearest integer

console.log(Math.floor(num3)) // Round down to the nearest integer

console.log(Math.max(num1, num2, num3)) // Maximum value among the numbers
console.log(Math.min(num1, num2, num3)) // Minimum value among the numbers;


console.log(Math.pow(4,3)) // 2 raised to the power of 3
console.log(Math.sqrt(64)) // Square root of 64;

console.log(Math.random()) // Generate a random number between 0 and 1

console.log(Math.random())

min = 1
max = 6

console.log(Math.floor(Math.random() * (max - min + 1)) + min) // Random number between 1 and 6

console.log(Math.PI) // Value of Pi
console.log(Math.E) // Value of Euler's number

console.log(Math.sign(num3)) // Sign of num2 (-1 for negative, 0 for zero, 1 for positive)

console.log(Math.trunc(num3)) // Truncate the decimal part of num3
console.log(Math.hypot(5, 10)) // Calculate the hypotenuse of a right triangle with sides 3 and 4


console.log(Math.round(Math.exp(5))) // Exponential function e^2;
